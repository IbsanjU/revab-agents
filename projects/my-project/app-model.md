# App model — my-project

Living map of the target application (pages, flows, selectors, environments).
Agents update this as they learn the app; every entry needs a source citation
(Jira key, Confluence page id, or exploration session date).

_This is a placeholder template — replace `my-project` with a real project._
