# Learnings

Persistent, dated session learnings. Agents append here (via the artifacts `knowledge_append` MCP tool or direct edit) and read this before starting work. Keep entries short and factual; delete entries proven wrong.

Entry format:

### YYYY-MM-DD
- What was learned / decided / failed and why.

---

### 2026-07-21
- Agents wandered across VS Code models because their rules were *referenced*, not present: every persona deferred conduct to `.github/copilot-instructions.md` "which loads automatically" — true only in default Copilot, so on other models the agent ran with no hard rules. Fix: rules are now **inlined** into each generated persona.
- Adopted a "md → code" prompt system (mirrors Claude Code's content/registration split): agents are typed specs in `prompts/agents/*.ts`; `prompts/shared/{hard-rules,conduct,skeleton}.ts` hold the single source; `scripts/build-prompts.ts` (`npm run build:prompts`, `-- --check` in CI) regenerates `.github/agents/*.agent.md`, `AGENTS.md`, `.github/copilot-instructions.md`, `CLAUDE.md`. **Never hand-edit a generated file** — edit the spec and rebuild.
- Portable tool vocabulary (`Read/Grep/Glob/Edit/Write/Bash/Task/WebFetch` + `mcp__server__tool`); the portable→host (Copilot) mapping lives once in `prompts/shared/skeleton.ts`. Per-agent tool lists are now minimal (no wildcards); orchestrator holds no exec/write tools so it must delegate.
- Copied Claude's coercion utils to `utils/semanticBoolean.ts` + `semanticNumber.ts` (zod v3 adaptation) and wrapped the jira server's boolean/`dryRun` fields — a quoted `"dryRun":"false"` now coerces to `false` instead of erroring/defeating the guard. Follow-up: roll the same wrapping out to confluence/jtmf/notify and to number fields; upgrade remaining MCP tool descriptions per `PROMPT_STYLE.md`.

### 2026-07-02
- Framework scaffolded: 4 local MCP servers (jira 7311, confluence 7312, jtmf 7313, artifacts 7314), file-queue orchestrator, Playwright+Cucumber+Allure skeleton, 7 agent chat modes.
- JTMF custom fields are org-specific: set `JTMF_STEPS_FIELD` in `.env` before test-case tools return steps.

### 2026-07-04
- Major restructure: revab-agents is now framework-only. Removed in-repo `tests/`, `cucumber.js`, and Playwright/Cucumber/Allure devDependencies — this repo never executes tests against itself.
- Added `projects.manifest.json` + `utils/manifest.ts` (zod) as the trust boundary for resolving which target-repo directory any tool/task may operate on; supports both a local `repoPath` and clone-on-demand from `repoUrl` into `.workspaces/<project>/`.
- Added `fixtures/sample-target-repo/` (project name `sample`) — a minimal Playwright+Cucumber+Allure project used only to smoke-test the new MCP tools; not part of this framework's own build/test.
- New project-scoped MCP servers: `playwright-runner` (7316), `allure-report` (7317, replaces `artifacts`' old `allure_summary`), `codegen` (7318, scaffolds features/steps/pages into a target repo and requires a `source` citation on every feature file).
- Added write tools (dryRun-by-default): `jtmf_create_test_case`, `jtmf_update_test_case`, `jira_update_issue`, `jira_transition_issue`. Added `apiPut` helper to `mcp-servers/shared/http.ts`.
- Added skills: `detect-execution-convention` (BrowserStack only if already present in the target repo, never assumed), `upload-to-jtmf`, `update-jira-epic`, `extract-requirements-from-image`, `extract-requirements-from-video`, `consolidate-project-report`, `build-test-plan-interactive`.
- Known gap: no OCR/speech-to-text MCP tool yet — image/video extraction skills currently depend on native vision capability or manually supplied transcripts. Candidate future work if multi-modal extraction needs to be fully automated.
- Orchestrator handlers (`run-bdd`, `generate-report`) now require `payload.project` and execute with `cwd` resolved from the manifest; `typecheck` remains scoped to this framework repo.

### 2026-07-09
- Self-review follow-up: framework now has real tests. `npm test` runs Node's built-in test runner (`node --import tsx --test`) over `utils/manifest.test.ts`, `orchestrator/queue.test.ts`, and `scripts/check-conventions.test.ts` (27 tests). Tests use env-var overrides (`MANIFEST_PATH_OVERRIDE`, `QUEUE_ROOT_DIR`) + cache-busted dynamic imports to isolate module state per test.
- `orchestrator/queue.ts` now has crash-recovery: `reclaimStale(staleMs)` requeues tasks stuck in `running` past a threshold (default 10min), tracked via `task.reclaimCount`; after `MAX_RECLAIMS` (3) it moves the task to `failed` instead of looping forever. Worker sweeps every `WORKER_STALE_SWEEP_MS` (default 60s); `npm run task -- reclaim [staleMs]` triggers it manually.
- Fixed a real pre-existing bug found while testing: `enqueue()` task ids could collide/misorder within the same millisecond (relied on `Date.now()-randomUUID` string sort for FIFO). Now includes a monotonic per-process sequence number in the id.
- Hardened `agents/registry.ts`'s `import-agents` handler: `payload.source` is now checked against a shell-metacharacter denylist and must resolve to an existing directory before being passed to `execCommand("npm", ...)` (which uses `spawn(..., { shell: true })` — argv entries there are not immune to injection).
- Added `npm run check:conventions` (`scripts/check-conventions.ts`): validates every `skills/*/SKILL.md` frontmatter (`name` matches dir, non-empty `description`) and that every `agents/registry.ts` handler calling `resolveProjectRepoPath(...)` first guards on `payload.project`.
- Added `npm run knowledge:rotate` (`scripts/rotate-learnings.ts`): archives all-but-current-month dated entries from `knowledge/learnings.md` into `knowledge/learnings/<YYYY-MM>.md` once the main file exceeds ~8KB; no-op below that threshold.
- Deferred: a dedicated OCR/speech-to-text MCP server (still the biggest documented capability gap) — not built this pass, flagged as the highest-leverage next server if image/video requirement extraction becomes a frequent real workflow.

### 2026-07-12
- Hardening pass landed: shared HTTP-server helper hardening, `knowledge_search` artifacts tool, docs-drift lint in `check:conventions` (every registered MCP tool name must appear in both `README.md` and `knowledge/memory.md`; every server dir must be in `.vscode/mcp.json`), and a CI workflow (`.github/workflows/ci.yml`: typecheck + test + check:conventions) with explicit least-privilege `permissions`.
- CodeQL findings fixed and verified clean: `utils/diagramParse.ts` entity decoding rewritten as a single-pass regex replace (avoids double-decoding/incomplete-sanitization class of bugs); CI workflow given explicit `permissions` block.
- Full validation green on this branch: `npm run typecheck`, `npm test` (57 tests), `npm run check:conventions`, CodeQL (0 alerts).

### 2026-07-11
- Removed `fixtures/sample-target-repo/` entirely — this is a generic agent framework meant to operate on any external target project or standalone, not something that needs a bundled sample project to demonstrate itself. `projects.manifest.json` now ships a `my-project` placeholder entry (`repoPath`/`repoUrl` both to be filled in by the adopter) since the schema requires at least one project. Updated `README.md`, `.github/copilot-instructions.md`, `agents/registry.ts` payload examples, and `knowledge/memory.md` accordingly.

### 2026-07-12 (prompt-pattern hardening)
- Adopted production-prompt patterns (as patterns only — no external prompt text copied): every `skills/*/SKILL.md` now has a fixed **Output** contract section; `.github/copilot-instructions.md` gained Good/Bad example pairs for hard rules #8/#9/#10 and a shared **Agent conduct** section (tool discipline, when-blocked escalation template, verbosity calibration, executing-agent completion checklist, anti-hallucination anchors, memory hygiene, per-agent can/cannot/must-not boundaries).
- Deferred: propagating/echoing the shared Agent conduct sections into the individual `.github/agents/*.agent.md` personas — the cloud execution environment cannot access that directory; do this in a local session if per-persona tightening is wanted. Plan: `knowledge/plans/framework/2026-07-12-prompt-pattern-hardening.md`.
- Follow-up landed: `npm run agents:conduct` (`scripts/apply-agent-conduct.ts`) appends the per-persona Conduct/Boundaries sections (plus completion checklist / when-blocked / anti-hallucination / memory-hygiene / verbosity blocks where applicable) into `.github/agents/*.agent.md`. Idempotent via a `<!-- shared-conduct:v1 -->` marker; `--dry-run` previews. Run it locally once — the cloud agent cannot write into `.github/agents/`.
- Drift guard landed: `check:conventions` now fails if any `.github/agents/*.agent.md` is missing the current shared-conduct marker (read live from `scripts/apply-agent-conduct.ts`, so version bumps propagate) or if the persona set diverges from the "Per-agent boundaries" list in `.github/copilot-instructions.md`. CI stays red until the local `npm run agents:conduct` run above is committed — intentional, so the pending state can't silently drift. Marker version-bump convention documented in `knowledge/conventions.md`.

### 2026-07-12 (conduct adaptations from reference prompts)
- Compared `.github/copilot-instructions.md`'s shared **Agent conduct** section against patterns (paraphrased, not copied) from a local reference collection of other coding-agent system prompts (`~/Documents/github/system_prompts_leaks`, outside this repo). Adopted 3 of 5 candidates: (1) a new universal **Faithful reporting** subsection — report outcomes exactly as observed, don't imply success by omission, prioritize accuracy over agreeableness (especially binding for reporter/researcher); (2) hard rule #10 now states approval is scoped to the previewed payload only — a prior "yes" never authorizes the same/similar write again later; (3) **Memory hygiene** now requires verifying a recalled entry's named file/selector/endpoint/flag still matches current state before acting on it. Not adopted (out of scope for this pass): task-tool-style todo tracking, a `report_intent`-style tool.
- Bumped `shared-conduct` marker `v1` → `v2` and fixed a real bug in `scripts/apply-agent-conduct.ts`: the skip check only compared against the exact current `MARKER` string, so a version bump would have *appended* a second `## Conduct` block instead of replacing the stale one (the `--dry-run`/docs claim of "replaces stale blocks" wasn't actually implemented). Now strips any `<!-- shared-conduct:vN -->` block (any version) through EOF before appending the new one — verified via `npm run agents:conduct` producing "replaced" (not "updated") for all 9 personas with no duplication.
- Also tightened `planner.agent.md`'s ambiguity rule into an explore-first procedure: exhaust `knowledge_search`/manifest/app-model/Jira-Confluence-JTMF search before asking the user anything discoverable from those sources; for genuine tradeoffs, offer 2–4 options with one marked recommended and proceed with it (recorded as an assumption) if the user doesn't respond.

### 2026-07-12 (dev-mode toggle + persona propagation)
- Added a `.devmode` marker-file convention (empty file at repo root, gitignored, local-only): its presence waives hard rule #13's planner-first gate specifically for direct `.github/agents/**` edits, so persona work doesn't need a separate saved plan while actively iterating on the framework. Absence → rule #13 applies normally, including to agent personas. Documented in `.github/copilot-instructions.md` (rule #13), `knowledge/conventions.md`, and `knowledge/memory.md`; created and currently enabled.
- Propagated the change into the two personas whose own prose restates rule 13: `planner.agent.md`'s intro and `orchestrator.agent.md`'s "Planner-first (hard rule 13)" section now both note the `.devmode` exception. `documenter.agent.md`'s rule-13 mention was left untouched — it's about Confluence documentation work, not agent-file edits, so the exception doesn't apply there.
- Folded in a session-derived learning: `orchestrator.agent.md`'s MCP-servers rule now notes that a VS Code MCP panel showing all servers "errored" while tool calls actually succeed usually means stale connection state, not dead processes — check `curl localhost:<port>/health` before re-running `npm run serve:mcp` (which just fails with `EADDRINUSE` if they're already up); the fix is reconnecting/reloading the editor, not restarting servers.
- Verified: `check:conventions` and `typecheck` both green after these persona edits.

### 2026-07-12 (removed the shared-conduct propagation mechanism entirely)
- User pushback: the per-persona `## Conduct` blocks (Boundaries, Faithful reporting, Completion checklist, Anti-hallucination, Memory hygiene, Verbosity) echoed into all 9 `.github/agents/*.agent.md` files were "unnecessary duplicate instructions." On inspection this was correct: `.github/copilot-instructions.md` loads automatically alongside every persona regardless of which agent is active, so every propagated block was 100% redundant with text already in its "Agent conduct" and "Per-agent boundaries" sections — the propagation added maintenance cost (marker-bump dance, the `apply-agent-conduct.ts` script, the duplication bug fixed earlier this session) without the model ever seeing new information.
- Removed entirely (reverting the intent of commits `e3a365b`/`ad0eff3`, not just simplifying): deleted `scripts/apply-agent-conduct.ts`, the `agents:conduct` npm script, and `check:conventions`' rule 4 (`checkAgentConduct`, both the marker check and its 4 tests in `scripts/check-conventions.test.ts`). All 9 persona files now end with a single one-line `## Conduct` pointer to `.github/copilot-instructions.md` instead of a duplicated block. `knowledge/conventions.md`/`knowledge/memory.md` updated to match; the persona-boundaries-list-parity idea was dropped along with the rest rather than kept standalone, per the user's "remove this" instruction.
- Verified: `npm test` (57/57, 4 fewer than before — the removed agent-conduct tests), `check:conventions` OK, `typecheck` clean.
- Lesson for future sessions: before propagating/duplicating any shared rule text into multiple files "for emphasis," check whether the reader (human or model) already sees the canonical source unconditionally — if so, duplication is pure maintenance liability, not redundancy-as-safety-net.

### 2026-07-12 (removed the .devmode carve-out too — reverted to pre-23655b5 rule 13)
- User traced the friction back further: "until commit `0e6e40c` everything was good" — that merge included `286ba3d` (planner agent + hard rule 13 in their original, simple form). Everything added after it (`23655b5` Agent-conduct section, `e3a365b`/`ad0eff3` the propagation+drift-check machinery just removed above) was the actual source of complexity, not rule 13 itself.
- The `.devmode` marker (added earlier this same session) was a reactive patch for friction that turned out to be caused by the propagation system, not by rule 13's planner-first gate. With the propagation system gone, the narrow `.devmode` carve-out for `.github/agents/**` no longer serves a real purpose — direct, explicit user instruction in an interactive chat session has been the operating approval mechanism all along (used for every edit this whole session, well beyond just agents/**), so a separate file-based toggle scoped to one path was redundant with practice.
- Removed: the `.devmode` file itself, its `.gitignore` entry, and every reference to it — rule #13 in `.github/copilot-instructions.md`, `planner.agent.md`'s intro, `orchestrator.agent.md`'s "Planner-first (hard rule 13)" section, and the `knowledge/conventions.md`/`knowledge/memory.md` bullets describing it — all reverted to their pre-devmode wording. Rule 13 is now textually the same as it was at `0e6e40c`.
- Verified: `check:conventions` and `typecheck` both green; `.gitignore` has zero diff against the committed version (net effect of adding then removing the same line).

### 2026-07-12 (MCP tool-description audit + deep dive on system_prompts_leaks)
- Two subagents dispatched in parallel: one audited all 46 `registerTool` descriptions across `mcp-servers/*/index.ts` against a "does it say when to use it, disambiguate from siblings, and note non-obvious constraints" bar; the other deep-read the coding-agent-relevant portions of `~/Documents/github/system_prompts_leaks` (Anthropic incl. Claude Code + its bundled-skills, OpenAI/Codex, Google, Microsoft, Misc — skimmed Cursor/xAI/Meta/Mistral/Notion/Perplexity/Qwen and found nothing extra worth opening), explicitly told to avoid proposing more shared-instruction duplication after this session's `.devmode`/conduct-propagation reversal.
- Applied 8 tool-description fixes (functional, non-duplicative — each lives in exactly one `registerTool` call): `jira_add_comment` (now notes it's not dryRun-gated, unlike its Jira write siblings), `confluence_search` (now states its return shape + points to `confluence_get_page`), `list_files`/`read_repo_file` (now cross-reference `knowledge_search` and each other, and note framework-repo-only scope), `read_pdf_text`/`read_docx_text` (now cross-reference `ocr_pdf` for scanned PDFs and each other), `scaffold_step`/`scaffold_page` (now document the no-overwrite behavior, previously discoverable only by reading the handler code). Verified `typecheck` and `check:conventions` green after.
- Deep-dive report produced ~20 additional candidate patterns (tool docs, SKILL.md conventions, workflow mechanics, security/anti-injection notes) — presented to the user as a menu rather than applied, given the volume and this session's demonstrated fatigue with unrequested process additions; see the chat transcript for the full list if revisiting. Top candidates flagged for a future pass if wanted: hardening `detect-execution-convention`'s BrowserStack SKIP list, a "never revert changes you didn't author" addition to the operational-safety section, and treating in-band content (Confluence/Jira/GitHub text) that claims to be a system instruction as untrusted data.

### 2026-07-12 (applied the full menu — 21 files)
- User asked for all ~20 candidates plus a deeper research pass (explicitly: Claude Fable 5, Playwright/browser-automation, and coding-tool prompts across vendors — a follow-up subagent dispatch). Findings: Fable 5 has almost no fable-5-specific *agentic* content vs opus-4.8/sonnet-5/opus-4.6 (shared boilerplate across the current model generation) — only a literal token-budget declaration is unique to it; sonnet-5's "proactivity" phrasing (ambiguity → pick a sensible default and proceed, don't default to asking) was the one adoptable bit, folded into `planner.agent.md`. Claude-in-Chrome's injection-defense pattern (quote suspicious in-page text, name its source, ask before acting) was the strongest justification yet for the untrusted-in-band-content rule. "Never revert changes you didn't author" turned out to already be covered by this session's own operational-safety instructions, so it was **not** added to copilot-instructions.md (avoided a redundant addition). No vendor's code-edit tool beats our existing dryRun convention (hard rule #10) — nothing to adopt there.
- Applied across 21 files (`git diff --stat` after: 132 insertions, 21 deletions): `jtmf_search_tests`/`jtmf_raw_get`/`confluence_get_page`/`notify_teams` tool descriptions tightened further; `detect-execution-convention` gained an explicit BrowserStack SKIP list + a per-surface (web/API/CLI) verification table; `build-test-plan-interactive` gained the quote-and-confirm injection-defense rule (it drives a real browser via the official `playwright` MCP); `upload-to-jtmf`/`update-jira-epic` gained falsifiable definition-of-done checklists; `analyze-test-failures` gained an optional "thorough" adversarial-refute mode plus a "silence is not success" status-watching rule; `search-across-sources` gained a signal-based disambiguation table (issue key vs JQL vs bare topic vs URL); `copilot-instructions.md` gained the in-band-untrusted-content anti-hallucination bullet and worked Good/Bad examples for rule #13; `automation.agent.md` gained an explicit run-bdd-routing note and a "green tests ≠ verified behavior" step; `playwright-bdd.instructions.md` gained a same-file-parallel-scaffold-write race-condition rule; `self-improve.agent.md`'s tool-allowlist-upgrade step now says to mine actually-observed tool usage rather than reason abstractly (no new "transcript mining" skill built — this repo has no tool-call logging infrastructure to mine yet, so the technique was folded into existing guidance instead of inventing unused plumbing); `knowledge/conventions.md` gained a new "Design heuristics" section (batch tool loads, pipeline-by-default/barrier-only-when-justified, rotation-is-archive-only-so-nothing-is-lost, detect-before-writing-refuse-on-ambiguity, security-review noise-suppression calibration); `orchestrator/worker.ts` got a comment clarifying `WORKER_POLL_MS` has no LLM-cache-window tradeoff to worry about (unlike the vendor pattern that inspired the question), just queue I/O/rate-limit costs.
- Verified: `typecheck`, `check:conventions`, and `npm test` (57/57) all green after the full batch.
- Lesson: when a research pass surfaces "add this rule to the shared conduct section" ideas, checking whether the same protection *already exists elsewhere* (this session's own operational-safety instructions, in this case) avoids re-introducing exactly the duplication problem fixed earlier today.

### 2026-07-18 (full agents/skills review + batch-2 merge + staged-work fixes)
- Reviewed the large staged batch (new `git` MCP server on :7322 — `git_log`/`git_branches`/`git_search`/`git_diff`/`git_show`, spawn shell:false, read-only, manifest-resolved; 9 new skills: `verify`, `code-review`, `simplify`, `security-review`, `onboard-project`, `review-against-spec`, `structure-project-data`, `data-visualization` (+ palette validator script), plus the BSA suite refinements; agent updates wiring them in). Quality was high; mechanical gates were already green.
- Fixed in review: (1) phantom "hard rule 14" citations in `onboard-project`/`review-against-spec`/`structure-project-data` → corrected to rule 9 (citation required) — there are only 13 hard rules; (2) `automation.agent.md` workflow used `allure_summary` and the `verify` skill (which drives real flows via the `playwright` MCP and reads Allure results) but its `tools:` list lacked `allure-report` and `playwright` — both added; (3) `onboard-project` and `review-against-spec` lacked the repo-standard `## Output` contract sections — added; (4) `reporter.agent.md` publish step now points chart work at the `data-visualization` skill (whose own description targets reporter, but reporter never referenced it); (5) `orchestrator.agent.md` specialists roster was missing `bsa` and `documenter`, and its new-project path now routes through the `onboard-project` skill.
- Merged stash@{0} (batch-2: 12 tool-description tightenings + 5 skill hardenings + playwright-bdd concurrency rule + conventions design-heuristics + worker comment) back on top of the staged work. Two conflicts, both resolved: `automation.agent.md` kept the staged workflow (its `verify`-skill step supersedes batch-2's inline "green tests ≠ verified" step — same intent, fuller realization); `confluence_search`'s description merged both sides (return shape + get_page pointer from batch-2, lastUpdated freshness note from staged).
- Verified after everything: `typecheck`, `check:conventions`, `npm test` (57/57) all green; no conflict markers left.
- Lesson: when two parallel work streams (a stash and a staged batch) both evolve the same idea, the later/fuller realization wins and the earlier inline version should be dropped in the merge — keeping both would have duplicated the verification guidance in two places in the same file.
