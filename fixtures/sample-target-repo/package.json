{
  "name": "sample-target-repo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal Playwright + Cucumber + Allure project used to smoke-test revab-agents MCP tools (playwright-runner, allure-report, codegen) against an external repo. Not part of the revab-agents framework itself.",
  "scripts": {
    "test:bdd": "cross-env NODE_OPTIONS=--import=tsx cucumber-js",
    "report:generate": "allure generate reports/allure-results -o reports/allure-report --clean",
    "report:open": "allure open reports/allure-report"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.8.0",
    "@playwright/test": "^1.45.0",
    "allure-commandline": "^2.29.0",
    "allure-cucumberjs": "^2.15.1",
    "cross-env": "^7.0.3",
    "tsx": "^4.15.0",
    "typescript": "^5.5.0"
  }
}
